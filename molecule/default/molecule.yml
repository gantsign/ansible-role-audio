---
dependency:
  name: galaxy

driver:
  name: docker

lint: |
  set -e
  yamllint .
  ansible-lint
  flake8

platforms:
  - name: ansible-role-audio-01
    image: ubuntu:16.04

provisioner:
  name: ansible

verifier:
  name: testinfra
